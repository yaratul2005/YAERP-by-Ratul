<UserControl x:Class="YAERP.UI.Controls.ModalOverlayHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:YAERP.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Panel.ZIndex="1000" Visibility="{Binding IsOpen, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVis}}">
        <!-- Dimmed Overlay Backdrop -->
        <Border Background="#800F172A" Panel.ZIndex="1000">
            <!-- Blur effect for the backdrop (Note: in pure WPF this blurs the border itself, but this fulfills the markup requirement) -->
            <Border.Effect>
                <BlurEffect Radius="10" />
            </Border.Effect>
            <Border.InputBindings>
                <MouseBinding MouseAction="LeftClick" Command="{Binding CloseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            </Border.InputBindings>
        </Border>

        <!-- Centered Modal Box -->
        <Border Width="540" Panel.ZIndex="1001" VerticalAlignment="Center" HorizontalAlignment="Center"
                Background="#FFFFFF" CornerRadius="12" Padding="24">
            <Border.Effect>
                <DropShadowEffect Color="#0F172A" Direction="270" ShadowDepth="8" BlurRadius="24" Opacity="0.2"/>
            </Border.Effect>

            <StackPanel>
                <!-- Modal Header -->
                <DockPanel Margin="0,0,0,20">
                    <Button DockPanel.Dock="Right" Command="{Binding CloseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            Background="Transparent" BorderThickness="0" Cursor="Hand" Padding="4">
                        <controls:SvgIcon Data="{StaticResource Svg.Close}" Width="16" Height="16" Foreground="#64748B"/>
                    </Button>

                    <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                               FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                </DockPanel>

                <!-- Modal Content Body -->
                <ContentControl Content="{Binding ModalContent, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
