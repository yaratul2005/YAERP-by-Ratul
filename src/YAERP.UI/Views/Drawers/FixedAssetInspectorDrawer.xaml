<UserControl x:Class="YAERP.UI.Views.Drawers.FixedAssetInspectorDrawer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:YAERP.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="440"
             Background="#FFFFFF">

    <StackPanel Margin="16">
        <!-- Drawer Header Banner -->
        <Border Background="#F1F5F9" CornerRadius="8" Padding="14" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <controls:SvgIcon Data="{StaticResource Svg.Finance}" Width="20" Height="20" Foreground="#2563EB" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <StackPanel>
                    <TextBlock Text="{Binding AssetTag, StringFormat='Fixed Asset Inspector: {0}'}" FontSize="15" FontWeight="Bold" Foreground="#0F172A"/>
                    <TextBlock Text="{Binding AssetName}" FontSize="12" Foreground="#64748B"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Status Message Banner -->
        <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0,0,0,12"
                Visibility="{Binding StatusMessage, Converter={StaticResource StringToVisConverter}}">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#2563EB" FontSize="12" FontWeight="SemiBold"/>
        </Border>

        <!-- Asset Progress Life Bar Box -->
        <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,16">
            <StackPanel>
                <DockPanel Margin="0,0,0,4">
                    <TextBlock Text="Asset Life Consumption %" FontSize="12" FontWeight="Bold" Foreground="#475569"/>
                    <TextBlock Text="{Binding LifeConsumedPercent, StringFormat='{}{0:F1}% Depreciated'}" FontSize="12" FontWeight="Bold" Foreground="#2563EB" HorizontalAlignment="Right"/>
                </DockPanel>

                <ProgressBar Value="{Binding LifeConsumedPercent, Mode=OneWay}" Minimum="0" Maximum="100" Height="10" Margin="0,4,0,6"/>

                <DockPanel>
                    <TextBlock Text="{Binding BookValue, StringFormat='Book Value: {0:C0}'}" FontSize="11" Foreground="#059669" FontWeight="Bold"/>
                    <TextBlock Text="{Binding AcquisitionCost, StringFormat='Cost: {0:C0}'}" FontSize="11" Foreground="#64748B" HorizontalAlignment="Right"/>
                </DockPanel>
            </StackPanel>
        </Border>

        <!-- Asset Master Fields -->
        <UniformGrid Columns="2" Margin="0,0,0,12">
            <StackPanel Margin="0,0,6,0">
                <TextBlock Text="Asset Tag Ref:" FontSize="12" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,4"/>
                <TextBox Text="{Binding AssetTag}" IsReadOnly="True" Height="34" Padding="6,4" Background="#F1F5F9"/>
            </StackPanel>

            <StackPanel Margin="6,0,0,0">
                <TextBlock Text="Asset Category:" FontSize="12" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Category}" IsReadOnly="True" Height="34" Padding="6,4" Background="#F1F5F9"/>
            </StackPanel>
        </UniformGrid>

        <!-- Depreciation Schedule Grid -->
        <TextBlock Text="Projected Depreciation Schedule Table:" FontSize="12" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,6"/>
        <DataGrid ItemsSource="{Binding ScheduleLines}" AutoGenerateColumns="False" CanUserAddRows="False" Height="140" Margin="0,0,0,20" GridLinesVisibility="Horizontal" BorderBrush="#E2E8F0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Period" Binding="{Binding Period}" Width="90" FontWeight="Bold"/>
                <DataGridTextColumn Header="Beginning Book" Binding="{Binding BeginningBookValue, StringFormat=C0}" Width="110"/>
                <DataGridTextColumn Header="Expense" Binding="{Binding DepreciationExpense, StringFormat=C0}" Width="100" Foreground="#DC2626" FontWeight="Bold"/>
                <DataGridTextColumn Header="Ending Book" Binding="{Binding EndingBookValue, StringFormat=C0}" Width="110"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Action Bar -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Command="{Binding CancelCommand}" Content="Cancel" Height="40" Background="#F1F5F9" Foreground="#475569" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
            
            <Button Grid.Column="2" Command="{Binding ExecuteDepreciationRunCommand}" Height="40" Background="#2563EB" Foreground="#FFFFFF" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                <StackPanel Orientation="Horizontal">
                    <controls:SvgIcon Data="{StaticResource Svg.Finance}" Width="16" Height="16" Foreground="#FFFFFF" Margin="0,0,6,0"/>
                    <TextBlock Text="Post Depreciation" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>
    </StackPanel>
</UserControl>
