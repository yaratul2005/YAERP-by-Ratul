<UserControl x:Class="YAERP.UI.Views.Drawers.BinTransferDrawer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:YAERP.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="440"
             Background="#FFFFFF">

    <StackPanel Margin="16">
        <!-- Drawer Header Banner -->
        <Border Background="#F1F5F9" CornerRadius="8" Padding="14" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <controls:SvgIcon Data="{StaticResource Svg.Transfer}" Width="20" Height="20" Foreground="#2563EB" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <StackPanel>
                    <TextBlock Text="Stock Relocation Transfer" FontSize="15" FontWeight="Bold" Foreground="#0F172A"/>
                    <TextBlock Text="Move items between warehouse bins with real-time audit log" FontSize="12" Foreground="#64748B"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Status Message Banner -->
        <Border Background="#FEF2F2" BorderBrush="#FCA5A5" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,16"
                Visibility="{Binding StatusMessage, Converter={StaticResource StringToVisConverter}}">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#991B1B" FontSize="12" FontWeight="SemiBold"/>
        </Border>

        <!-- Source Bin Picker -->
        <TextBlock Text="Source Warehouse Bin:" FontSize="12" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,6"/>
        <ComboBox ItemsSource="{Binding SourceBins}" SelectedItem="{Binding SelectedSourceBin}" DisplayMemberPath="BinCode" Height="36" Margin="0,0,0,16"/>

        <!-- Destination Bin Picker -->
        <TextBlock Text="Destination Warehouse Bin:" FontSize="12" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,6"/>
        <ComboBox ItemsSource="{Binding DestinationBins}" SelectedItem="{Binding SelectedDestinationBin}" DisplayMemberPath="BinCode" Height="36" Margin="0,0,0,16"/>

        <!-- Product Name -->
        <TextBlock Text="Product SKU / Name:" FontSize="12" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,6"/>
        <TextBox Text="{Binding SelectedProductName, UpdateSourceTrigger=PropertyChanged}" Height="36" Padding="8,6" Margin="0,0,0,16"/>

        <!-- Transfer Quantity -->
        <TextBlock Text="Transfer Quantity:" FontSize="12" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,6"/>
        <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Height="36" Padding="8,6" Margin="0,0,0,16"/>

        <!-- Notes -->
        <TextBlock Text="Relocation Reason / Notes:" FontSize="12" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,6"/>
        <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="60" Padding="8,6" Margin="0,0,0,24"/>

        <!-- Actions -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Command="{Binding CancelCommand}" Content="Cancel" Height="40" Background="#F1F5F9" Foreground="#475569" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
            
            <Button Grid.Column="2" Command="{Binding ExecuteTransferCommand}" Height="40" Background="#2563EB" Foreground="#FFFFFF" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                <StackPanel Orientation="Horizontal">
                    <controls:SvgIcon Data="{StaticResource Svg.Transfer}" Width="16" Height="16" Foreground="#FFFFFF" Margin="0,0,6,0"/>
                    <TextBlock Text="Execute Transfer" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>
    </StackPanel>
</UserControl>
