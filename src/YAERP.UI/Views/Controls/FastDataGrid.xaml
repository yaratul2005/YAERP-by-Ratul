<UserControl x:Class="YAERP.UI.Views.Controls.FastDataGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0" x:Name="InnerGrid"
                  AutoGenerateColumns="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  VirtualizingPanel.ScrollUnit="Item"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  BorderThickness="1"
                  BorderBrush="#E2E8F0"
                  RowHeight="40"
                  Background="White"
                  ItemsSource="{Binding Items}">
        </DataGrid>

        <!-- Pagination and Export Footer -->
        <Border Grid.Row="1" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1,0,1,1" Padding="12,8">
            <DockPanel>
                <Button DockPanel.Dock="Right" Content="📥 Export CSV" Padding="12,4" Command="{Binding ExportCommand}"
                        Background="#10B981" Foreground="White" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="⏮ First" Command="{Binding FirstPageCommand}" Margin="0,0,4,0" Padding="8,4" Background="White" BorderBrush="#CBD5E1"/>
                    <Button Content="◀ Prev" Command="{Binding PrevPageCommand}" Margin="0,0,12,0" Padding="8,4" Background="White" BorderBrush="#CBD5E1"/>

                    <TextBlock Text="{Binding PageInfo}" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,12,0"/>

                    <Button Content="Next ▶" Command="{Binding NextPageCommand}" Margin="0,0,4,0" Padding="8,4" Background="White" BorderBrush="#CBD5E1"/>
                    <Button Content="Last ⏭" Command="{Binding LastPageCommand}" Padding="8,4" Background="White" BorderBrush="#CBD5E1"/>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
