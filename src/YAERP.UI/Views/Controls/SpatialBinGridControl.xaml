<UserControl x:Class="YAERP.UI.Views.Controls.SpatialBinGridControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
    </UserControl.Resources>

    <Grid Background="#F8FAFC">
        <DockPanel>
            <!-- Map Legend -->
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12" HorizontalAlignment="Right">
                <Border Background="#DCFCE7" BorderBrush="#86EFAC" BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="0,0,8,0">
                    <TextBlock Text="● Normal (&lt;75%)" FontSize="10" FontWeight="Bold" Foreground="#15803D"/>
                </Border>
                <Border Background="#FEF3C7" BorderBrush="#FDE68A" BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="0,0,8,0">
                    <TextBlock Text="● Near Capacity (75-95%)" FontSize="10" FontWeight="Bold" Foreground="#B45309"/>
                </Border>
                <Border Background="#FEE2E2" BorderBrush="#FCA5A5" BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="0,0,8,0">
                    <TextBlock Text="● Full (>95%)" FontSize="10" FontWeight="Bold" Foreground="#991B1B"/>
                </Border>
                <Border Background="#DBEAFE" BorderBrush="#93C5FD" BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="0,0,8,0">
                    <TextBlock Text="● Cold Storage" FontSize="10" FontWeight="Bold" Foreground="#1E40AF"/>
                </Border>
                <Border Background="#475569" CornerRadius="4" Padding="6,2">
                    <TextBlock Text="🔒 Locked Bin" FontSize="10" FontWeight="Bold" Foreground="#FFFFFF"/>
                </Border>
            </StackPanel>

            <!-- Visual Bin Tiles Grid -->
            <ItemsControl ItemsSource="{Binding SpatialBins}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="4"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="6" Padding="12" CornerRadius="8" BorderThickness="2" Cursor="Hand">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#DCFCE7"/>
                                    <Setter Property="BorderBrush" Value="#86EFAC"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsColdStorage}" Value="True">
                                            <Setter Property="Background" Value="#EFF6FF"/>
                                            <Setter Property="BorderBrush" Value="#93C5FD"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsLocked}" Value="True">
                                            <Setter Property="Background" Value="#F1F5F9"/>
                                            <Setter Property="BorderBrush" Value="#64748B"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <StackPanel>
                                <DockPanel Margin="0,0,0,6">
                                    <TextBlock Text="{Binding BinCode}" FontWeight="Bold" FontSize="13" Foreground="#0F172A"/>
                                    <Border Visibility="{Binding IsLocked, Converter={StaticResource BoolToVisConverter}}" HorizontalAlignment="Right" Background="#EF4444" CornerRadius="4" Padding="4,1">
                                        <TextBlock Text="🔒 LOCKED" FontSize="9" FontWeight="Bold" Foreground="#FFFFFF"/>
                                    </Border>
                                </DockPanel>

                                <TextBlock Text="{Binding ZoneType, StringFormat='Zone: {0}'}" FontSize="11" Foreground="#64748B" Margin="0,0,0,8"/>

                                <!-- Progress Bar Heatmap -->
                                <DockPanel Margin="0,2">
                                    <TextBlock Text="Utilization:" FontSize="10" Foreground="#475569"/>
                                    <TextBlock Text="{Binding UtilizationPercent, StringFormat='{}{0:F1}%'}" FontSize="10" FontWeight="Bold" HorizontalAlignment="Right"/>
                                </DockPanel>
                                <ProgressBar Value="{Binding UtilizationPercent}" Maximum="100" Height="8"/>

                                <DockPanel Margin="0,8,0,0">
                                    <Button Command="{Binding DataContext.OpenTransferDrawerCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Content="Relocate" Padding="6,2" FontSize="10" FontWeight="Bold" Background="#2563EB" Foreground="White" BorderThickness="0"/>
                                    <Button Command="{Binding DataContext.ToggleBinLockCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Content="Lock/Unlock" Padding="6,2" FontSize="10" FontWeight="Bold" Background="#64748B" Foreground="White" BorderThickness="0" HorizontalAlignment="Right"/>
                                </DockPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </DockPanel>
    </Grid>
</UserControl>
