<UserControl x:Class="YAERP.UI.Views.Modals.RejectionReasonModal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:YAERP.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="480"
             Background="#FFFFFF">

    <StackPanel Margin="8">
        <!-- Header Banner -->
        <Border Background="#FEF2F2" BorderBrush="#FCA5A5" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,14">
            <DockPanel>
                <controls:SvgIcon Data="{StaticResource Svg.Close}" Width="24" Height="24" Foreground="#DC2626" DockPanel.Dock="Left" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <StackPanel>
                    <TextBlock Text="{Binding EntityId, StringFormat='Reject Approval Request: #{0}'}" FontSize="16" FontWeight="Bold" Foreground="#991B1B"/>
                    <TextBlock Text="Manager justification comment is mandatory for audit trail logging" FontSize="12" Foreground="#B91C1C"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Status Message Banner -->
        <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0,0,0,12"
                Visibility="{Binding StatusMessage, Converter={StaticResource StringToVisConverter}}">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#DC2626" FontSize="12" FontWeight="SemiBold"/>
        </Border>

        <TextBlock Text="Rejection Reason / Justification:" FontSize="12" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,4"/>
        <TextBox Text="{Binding RejectionReason, UpdateSourceTrigger=PropertyChanged}" Height="100" AcceptsReturn="True" Padding="8" Margin="0,0,0,20"/>

        <!-- Action Bar -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Command="{Binding CancelCommand}" Content="Cancel" Height="40" Background="#F1F5F9" Foreground="#475569" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
            
            <Button Grid.Column="2" Command="{Binding ConfirmRejectionCommand}" Height="40" Background="#DC2626" Foreground="#FFFFFF" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                <StackPanel Orientation="Horizontal">
                    <controls:SvgIcon Data="{StaticResource Svg.Close}" Width="16" Height="16" Foreground="#FFFFFF" Margin="0,0,6,0"/>
                    <TextBlock Text="Confirm Rejection" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>
    </StackPanel>
</UserControl>
