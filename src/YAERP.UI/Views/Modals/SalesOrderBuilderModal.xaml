<UserControl x:Class="YAERP.UI.Views.Modals.SalesOrderBuilderModal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:YAERP.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="680"
             Background="#FFFFFF">

    <StackPanel Margin="8">
        <!-- Header Banner -->
        <Border Background="#F0FDF4" BorderBrush="#BBF7D0" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,14">
            <DockPanel>
                <controls:SvgIcon Data="{StaticResource Svg.Sales}" Width="24" Height="24" Foreground="#16A34A" DockPanel.Dock="Left" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <StackPanel>
                    <TextBlock Text="{Binding OrderNumber, StringFormat='Sales Order Builder: #{0}'}" FontSize="16" FontWeight="Bold" Foreground="#14532D"/>
                    <TextBlock Text="Line item list price lookups, gross margin thresholds &amp; automated regional tax calculation" FontSize="12" Foreground="#15803D"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Credit Alert Banner -->
        <Border Background="#FEF2F2" BorderBrush="#FCA5A5" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,12"
                Visibility="{Binding ExceedsCreditLimit, Converter={StaticResource BoolToVis}}">
            <StackPanel Orientation="Horizontal">
                <controls:SvgIcon Data="{StaticResource Svg.Lock}" Width="16" Height="16" Foreground="#DC2626" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="⚠️ Credit Warning: Order Grand Total exceeds customer available credit line!" Foreground="#991B1B" FontSize="12" FontWeight="Bold"/>
            </StackPanel>
        </Border>

        <!-- Status Message Banner -->
        <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0,0,0,12"
                Visibility="{Binding StatusMessage, Converter={StaticResource StringToVisConverter}}">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#2563EB" FontSize="12" FontWeight="SemiBold"/>
        </Border>

        <!-- Form Top Header -->
        <UniformGrid Columns="2" Margin="0,0,0,12">
            <StackPanel Margin="0,0,6,0">
                <TextBlock Text="Customer Picker:" FontSize="12" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,4"/>
                <ComboBox ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}" DisplayMemberPath="Name" Height="34"/>
            </StackPanel>

            <StackPanel Margin="6,0,0,0">
                <TextBlock Text="Order Number / Reference:" FontSize="12" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,4"/>
                <TextBox Text="{Binding OrderNumber, UpdateSourceTrigger=PropertyChanged}" Height="34" Padding="6,4"/>
            </StackPanel>
        </UniformGrid>

        <!-- Line Items Grid Toolbar -->
        <DockPanel Margin="0,0,0,6">
            <TextBlock Text="Order Line Items:" FontSize="13" FontWeight="Bold" Foreground="#0F172A" VerticalAlignment="Center"/>
            <Button Command="{Binding AddLineItemCommand}" DockPanel.Dock="Right" Content="+ Add Line Item" Padding="10,4" Background="#10B981" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
        </DockPanel>

        <!-- Line Items DataGrid -->
        <DataGrid ItemsSource="{Binding OrderItems}" AutoGenerateColumns="False" CanUserAddRows="False" Height="160" Margin="0,0,0,14" GridLinesVisibility="Horizontal" BorderBrush="#E2E8F0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Item Description" Binding="{Binding ProductName, UpdateSourceTrigger=PropertyChanged}" Width="180" FontWeight="Bold"/>
                <DataGridTextColumn Header="Qty" Binding="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Width="70"/>
                <DataGridTextColumn Header="Unit Price ($)" Binding="{Binding UnitPrice, UpdateSourceTrigger=PropertyChanged, StringFormat=F2}" Width="100"/>
                <DataGridTextColumn Header="Line Total ($)" Binding="{Binding LineTotal, StringFormat=C2}" Width="110" IsReadOnly="True"/>
                
                <!-- Gross Margin % Badge -->
                <DataGridTemplateColumn Header="Margin %" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Border Background="#FEF2F2" Visibility="{Binding IsLowMargin, Converter={StaticResource BoolToVis}}" CornerRadius="4" Padding="4,2" Margin="0,0,4,0">
                                    <TextBlock Text="⚠️ Low" FontSize="10" FontWeight="Bold" Foreground="#DC2626"/>
                                </Border>
                                <TextBlock Text="{Binding GrossMarginPercent, StringFormat='{}{0:F1}%'}" FontSize="11" FontWeight="Bold" Foreground="#475569"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Actions" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.RemoveLineItemCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Content="✕" Background="Transparent" Foreground="#DC2626" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Order Summary & Tax Breakdown Box -->
        <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="220"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Regional Tax Calculation:" FontSize="12" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,4"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Tax Rate (%): " FontSize="12" Foreground="#64748B" VerticalAlignment="Center"/>
                        <TextBox Text="{Binding TaxRatePercent, UpdateSourceTrigger=PropertyChanged}" Width="60" Height="28" Padding="4,2" Margin="6,0,0,0"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <DockPanel Margin="0,0,0,4">
                        <TextBlock Text="Subtotal:" Foreground="#64748B" FontSize="12"/>
                        <TextBlock Text="{Binding SubTotal, StringFormat=C2}" FontWeight="Bold" Foreground="#0F172A" HorizontalAlignment="Right"/>
                    </DockPanel>
                    
                    <DockPanel Margin="0,0,0,4">
                        <TextBlock Text="{Binding TaxRatePercent, StringFormat='Tax ({0}%):'}" Foreground="#64748B" FontSize="12"/>
                        <TextBlock Text="{Binding TaxAmount, StringFormat=C2}" FontWeight="Bold" Foreground="#0F172A" HorizontalAlignment="Right"/>
                    </DockPanel>

                    <Separator Background="#CBD5E1" Margin="0,4"/>

                    <DockPanel>
                        <TextBlock Text="Grand Total:" FontWeight="Bold" FontSize="14" Foreground="#0F172A"/>
                        <TextBlock Text="{Binding GrandTotal, StringFormat=C2}" FontWeight="Bold" FontSize="16" Foreground="#2563EB" HorizontalAlignment="Right"/>
                    </DockPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Action Bar -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Command="{Binding CancelCommand}" Content="Cancel" Height="40" Background="#F1F5F9" Foreground="#475569" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
            
            <Button Grid.Column="2" Command="{Binding SubmitOrderCommand}" Height="40" Background="#2563EB" Foreground="#FFFFFF" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                <StackPanel Orientation="Horizontal">
                    <controls:SvgIcon Data="{StaticResource Svg.Check}" Width="16" Height="16" Foreground="#FFFFFF" Margin="0,0,6,0"/>
                    <TextBlock Text="Submit Sales Order" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>
    </StackPanel>
</UserControl>
