<UserControl x:Class="YAERP.UI.Views.Modals.ShopFloorOutputModal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:YAERP.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="480" d:DesignWidth="480"
             Background="#FFFFFF">

    <StackPanel Margin="8">
        <!-- Header Banner -->
        <Border Background="#EFF6FF" BorderBrush="#BFDBFE" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,14">
            <DockPanel>
                <controls:SvgIcon Data="{StaticResource Svg.Manufacturing}" Width="24" Height="24" Foreground="#2563EB" DockPanel.Dock="Left" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <StackPanel>
                    <TextBlock Text="{Binding WorkOrderNumber, StringFormat='Shop Floor Output Entry: #{0}'}" FontSize="16" FontWeight="Bold" Foreground="#1E3A8A"/>
                    <TextBlock Text="Log completed units, scrap reasons &amp; direct operator labor hours" FontSize="12" Foreground="#3B82F6"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Status Message Banner -->
        <Border Background="#FEF2F2" BorderBrush="#FCA5A5" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0,0,0,12"
                Visibility="{Binding StatusMessage, Converter={StaticResource StringToVisConverter}}">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#991B1B" FontSize="12" FontWeight="SemiBold"/>
        </Border>

        <!-- Quantities Input -->
        <UniformGrid Columns="2" Margin="0,0,0,12">
            <StackPanel Margin="0,0,6,0">
                <TextBlock Text="Completed Units:" FontSize="12" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,4"/>
                <TextBox Text="{Binding CompletedQuantity, UpdateSourceTrigger=PropertyChanged}" Height="34" Padding="6,4" FontWeight="Bold" Foreground="#059669"/>
            </StackPanel>

            <StackPanel Margin="6,0,0,0">
                <TextBlock Text="Scrapped Units:" FontSize="12" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,4"/>
                <TextBox Text="{Binding ScrappedQuantity, UpdateSourceTrigger=PropertyChanged}" Height="34" Padding="6,4" FontWeight="Bold" Foreground="#DC2626"/>
            </StackPanel>
        </UniformGrid>

        <UniformGrid Columns="2" Margin="0,0,0,12">
            <StackPanel Margin="0,0,6,0">
                <TextBlock Text="Labor Hours Logged:" FontSize="12" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,4"/>
                <TextBox Text="{Binding LaborHours, UpdateSourceTrigger=PropertyChanged}" Height="34" Padding="6,4"/>
            </StackPanel>

            <StackPanel Margin="6,0,0,0">
                <TextBlock Text="Work Order Ref:" FontSize="12" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,4"/>
                <TextBox Text="{Binding WorkOrderNumber}" IsReadOnly="True" Height="34" Padding="6,4" Background="#F1F5F9"/>
            </StackPanel>
        </UniformGrid>

        <TextBlock Text="Scrap Reason Description:" FontSize="12" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,4"/>
        <TextBox Text="{Binding ScrapReason, UpdateSourceTrigger=PropertyChanged}" Height="60" AcceptsReturn="True" Padding="6,4" Margin="0,0,0,20"/>

        <!-- Action Bar -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Command="{Binding CancelCommand}" Content="Cancel" Height="40" Background="#F1F5F9" Foreground="#475569" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
            
            <Button Grid.Column="2" Command="{Binding SubmitProductionOutputCommand}" Height="40" Background="#2563EB" Foreground="#FFFFFF" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                <StackPanel Orientation="Horizontal">
                    <controls:SvgIcon Data="{StaticResource Svg.Check}" Width="16" Height="16" Foreground="#FFFFFF" Margin="0,0,6,0"/>
                    <TextBlock Text="Record Production Output" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>
    </StackPanel>
</UserControl>
